// Drizzle schema stub. This project queries via raw SQL (see src/lib/db.ts);
// this file exists only so drizzle.config.ts (used by `netlify database
// migrations` tooling) has a schema path. Migrations are hand-written SQL in
// netlify/database/migrations/.
